# Быстрый старт

Инструкция для самого быстрого запуска патчинга.

## 1. Подготовка (5 минут)

```bash
# Клонировать репозиторий
git clone <your-repo-url>
cd xdsdk-bypass

# Установить зависимости (Ubuntu/Debian)
sudo apt-get update
sudo apt-get install -y default-jdk python3
```

## 2. Патчинг (1 команда)

```bash
# Поместите ваш APK в папку
cp /path/to/app.apk ./app.apk

# Запустите патчинг
./scripts/full_pipeline.sh app.apk
```

Результат: `app-patched.apk`

## 3. Установка (1 минута)

```bash
# Подключите Android устройство по USB
# Включите USB debugging в Developer Options

# Установите пропатченный APK
adb install -r app-patched.apk
```

## Готово! 🎉

Приложение запустится без экрана ввода лицензии.

---

## Что делает патч?

- ✅ Убирает экран ввода лицензионного ключа
- ✅ Отключает проверки лицензии
- ✅ Сохраняет автообновления
- ✅ Все функции приложения работают

## Возможные проблемы

### "apktool: command not found"
```bash
# Скрипт установит автоматически при первом запуске
./scripts/decompile.sh app.apk
```

### "adb: command not found"
```bash
# Установите Android platform tools
sudo apt-get install -y adb
```

### APK не устанавливается
```bash
# Удалите старую версию
adb uninstall com.eternal

# Установите заново
adb install app-patched.apk
```

### Приложение крашится
```bash
# Посмотрите логи
adb logcat | grep -i "AndroidRuntime\|eternal"

# Или используйте тестовый скрипт
./scripts/test_apk.sh app-patched.apk
```

## Дальнейшие шаги

- 📖 Прочитайте [README.md](README.md) для подробностей
- 🔧 Изучите [TECHNICAL.md](docs/TECHNICAL.md) для технических деталей
- 💡 Смотрите [EXAMPLES.md](EXAMPLES.md) для продвинутых сценариев
- ❓ Проверьте [FAQ.md](docs/FAQ.md) для ответов на вопросы

## Команды для копирования

```bash
# Весь процесс одной командой
git clone <repo> && cd xdsdk-bypass && \
sudo apt-get install -y default-jdk python3 adb && \
./scripts/full_pipeline.sh /path/to/app.apk && \
adb install -r app-patched.apk
```

## Важные файлы

| Файл | Назначение |
|------|-----------|
| `scripts/full_pipeline.sh` | Полный автоматический патчинг |
| `scripts/test_apk.sh` | Тестирование APK на устройстве |
| `config.example.sh` | Пример конфигурации |
| `app-patched.apk` | Результат патчинга |

## Поддержка

- 🐛 Bug? → [Create Issue](../../issues)
- 💬 Question? → [Discussions](../../discussions)
- 📧 Security? → See [SECURITY.md](SECURITY.md)

---

**Время на патчинг:** ~5-10 минут (включая установку)

**Поддерживаемые устройства:** Android 7.0+ (ARM64)

**Требования:** Ubuntu/Debian Linux (или Docker)
